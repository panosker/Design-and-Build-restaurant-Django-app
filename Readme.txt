This application contains:
Django to serve static HTML content,
connect the backend to a MySQL database
menu and table booking APIs
user registration and authentication
unit tests #(for the views and models)
the API be tested with the Insomnia REST client

 http://127.0.0.1:8000/:   
                        admin/
                        api/menu-items/
                        restaurant/booking/
                        auth/users
                        auth/
                        /api/register/